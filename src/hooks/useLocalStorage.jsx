import { useState, useEffect } from 'react'

const PREFIX = "dev-sandbox-"

const useLocalStorage = (key, value) => {
    const prefixedValue = PREFIX + key

    
    return (
        <div>
            
        </div>
    )
}

export default useLocalStorage
